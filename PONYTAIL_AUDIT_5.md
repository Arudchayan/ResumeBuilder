# ponytail-audit 5 — ResumeBuilder

1. `delete:` e2e fixture `arudchayan-sample-2026.json` — zero references; smoke.spec loads only `arudchayan-resume.json`. Nothing. [apps/web/e2e/fixtures/arudchayan-sample-2026.json]
2. `yagni:` per-package tsc→dist build ceremony for private, never-published packages — vite already aliases every `@resume/*` to src and vitest resolves linked workspace sources natively, yet root build/pretest/typecheck chain six `--filter … build` steps. Point package `main`/`types` at `src/index.ts`, drop lib `build` scripts, collapse the three root scripts (web's `tsc --noEmit && vite build` covers the tree transitively; typecheck becomes `pnpm -r typecheck`). [package.json:10-15, packages/*/package.json]
3. `delete:` `peerDependencies` (react, react-dom) on private workspace packages ui + templates — nothing is published; devDeps pin React 19. Delete the blocks. [packages/ui/package.json, packages/templates/package.json]
4. `shrink:` three parallel setter mechanisms in `ResumeCommand` — typed `setTemplate`/`setTheme`/`setSectionVisibility`/`reorderSections`/`setSkills` cases alongside generic `setField` dot-paths that express all five (`setSkills` already has zero dispatchers). Keep `setField` + `setDocument`; retarget EditorPage callers, drop the five variants/cases. [packages/core/src/commands/applyCommand.ts:6-14,31-76, apps/web/src/pages/EditorPage.tsx]
5. `shrink:` `buildSingleColumn` builds sidebar blocks then filters them back OUT by string-matching (`text === "Profile"`, muted-heading sniff, summary compare). Share the section loop as `pushContentSections(target, doc)`; kill the post-filter. [packages/templates/src/ir/documentToIr.ts:216-259]
6. `delete:` dead exports/fields — core's `TEMPLATE_IDS`/`type Job`/`type Project`/`getDefaultVisibility` re-exports (unused outside core), `PageCrossing.sectionId` (UI prints `label` only), templates' `TemplateManifest`/`IrColumn`/`IrPage`/`PaperPreset` type re-exports (zero importers). Trim to what's imported. [packages/core/src/index.ts, packages/templates/src/index.ts, packages/templates/src/preview/paper.ts]
7. `shrink:` `themes[*].light` + `--theme-light` var — emitted by `themeCssVars`, consumed by nothing (surface IS consumed, keep it). Remove field + emission + styles.css default. [packages/ui/src/tokens/themes.ts, packages/ui/src/styles.css]
8. `shrink:` eight hardcoded theme `<option>`s in EditorPage duplicating the `themes` map keys/names. Render `Object.entries(themes)` — one source of truth. [apps/web/src/pages/EditorPage.tsx:485-492]
9. `native:` `crypto.randomUUID?.() ?? \`local-${Date.now()}\`` fallbacks — `randomUUID` is baseline everywhere supported (and store.ts already calls it bare). Plain `crypto.randomUUID()`. [packages/core/src/schema/sections.ts:31, packages/storage/src/index.ts:41]
10. `delete:` `exportStatus` state — PDF path nulls it one line after setting it; the disabled button + toasts already cover feedback. Nothing. [apps/web/src/pages/EditorPage.tsx:139,241,246,258,576,587]
11. `shrink:` `normalizeTemplateId` — legacy branch (`modern|classic|default`) returns `"sidebar"`, identical to the fallthrough; body collapses to an `includes` ternary. [packages/core/src/schema/resume.ts:139-145]
12. `shrink:` `await import("@resume/export")` inside `runExport` — the package is already statically imported by store.ts, so the dynamic import lazifies nothing. Static `downloadDocx` import. [apps/web/src/pages/EditorPage.tsx:243]
13. `shrink:` `exportResumeJson`/`importResumeJson` — near-alias wrappers around stringify/parse parked in storage. Inline into store.ts (`parseResumeData` directly); drop the wrapper test. [packages/storage/src/index.ts:124-131, packages/storage/src/__tests__/storage.test.ts:26-33]
14. `shrink:` `dispatch()` guard `if (next === history.present)` — `applyCommand` always returns a fresh `structuredClone`, so the branch is unreachable. Remove. [packages/core/src/history/history.ts:18]
15. `shrink:` `IrBlock.heading.level: 1 | 2 | 3` — nothing ever emits level 3; both renderers special-case only 1 vs rest. Narrow to `1 | 2`. [packages/templates/src/ir/documentToIr.ts:6]
16. `delete:` dead ternary `...(ir.templateId === "compact" ? {} : {})` in sheet padding spread — both arms empty. Nothing. [packages/templates/src/preview/ResumePreview.tsx:226]
17. `delete:` stale docs — `docs/architecture/ponytail-audit.md` ledger documents since-deleted seams (`@resume/ports`, immer, file-saver, `@react-pdf/renderer`, MemoryStorage); README repeats them (stack line, monorepo table rows, docs link). Delete ledger + fix README. [docs/architecture/ponytail-audit.md, README.md]

net: -320 lines, -0 deps possible.
