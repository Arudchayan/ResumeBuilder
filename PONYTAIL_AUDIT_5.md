# ponytail-audit 5 — ResumeBuilder

1. `delete:` dead CSS layer — `.rb-section`/`.skills-compact` (emitters died with the chips-variant collapse), `.ats-standard`/`.ats-compact` (zero emitters ever), `@keyframes fade-up` + `.animate-fade-up` + `--motion-med` (zero users), `--workspace-warm` (defined, never read), and ui's `.ui-button-danger` + `"danger"` in the Button variant union (zero `variant="danger"` calls; SectionEditor uses its own `button-danger` class). Nothing replaces them. ~-35 lines. [apps/web/src/styles.css:16,588-608,718-733, print block ~938; packages/ui/src/styles.css:8,65-71; packages/ui/src/components/primitives.tsx:16]
2. `delete:` unused cross-package devDeps — `react-dom`+`@types/react-dev` in ui and templates (no `react-dom` import in either src), `react`+`@types/react` in export (zero react imports in exportDocx.ts), `vitest` in web (no unit tests, no vitest config left there). Nothing; delete from manifests. -6 manifest entries / -4 npm deps. [packages/ui/package.json, packages/templates/package.json, packages/export/package.json, apps/web/package.json]
3. `yagni:` six-entry `resolve.alias` block + `path` import in vite config — every `@resume/*` manifest now points `main`/`exports` at `src/index.ts` (post-audit-4 change), and Vite honors `exports`; the aliases are dist-era scaffolding duplicating the exports map. Delete the whole `resolve` block. ~-28 lines. [apps/web/vite.config.ts:4,9-36]
4. `shrink:` `sectionCopy` map (12 entries) duplicates `SECTION_CONFIG` ids/labels, adding only descriptions — merge `description` into `SECTION_CONFIG` and have `SectionIntro` read it; one source of truth. ~-12 lines. [apps/web/src/components/SectionEditor.tsx:12-24]
5. `yagni:` per-package `build`/`lint`/`test` scripts (audit-4 #4 remainder) — nothing invokes them (root `build` filters `@resume/web` only; root `test` runs vitest over `packages/*/src` directly; per-package `lint` is byte-identical to its `typecheck`). Keep one `typecheck` per manifest. ~-17 lines. [packages/*/package.json, apps/web/package.json:11]
6. `shrink:` dist-era tsconfig ceremony — `declaration`/`declarationMap`/`sourceMap` (all compiles are `--noEmit`), dead-default `forceConsistentCasingInFileNames`, and per-package `outDir`/`rootDir`/`__tests__` exclude referencing the deleted tsc builds. Trim to shared strict options + include. ~-19 lines. [tsconfig.base.json:11,15-17, packages/{core,ui,templates,storage,export}/tsconfig.json]
7. `delete:` provably-dead exports/types — `PaperPreset` (zero refs), `type SectionId` (zero importers), barrel re-exports `ThemeId`, `IrColumn`, `TemplateManifest`, `PageBreakGuides` (each consumed only inside own package), `exportDocxBlob` (only caller is `downloadDocx` same file). Un-export/delete. ~-9 lines. [packages/templates/src/index.ts, packages/templates/src/preview/paper.ts, packages/ui/src/index.ts, packages/core/src/index.ts + sections.ts, packages/export/src/index.ts]
8. `delete:` `"@resume/ui": "workspace:*"` in export package — exportDocx.ts imports only `docx`, core, templates. Nothing. -1 dep. [packages/export/package.json]
9. `shrink:` `ResumeMeta.title`/`.template` computed but never read — sole consumer (`store.bootstrap`) uses `list[0].id` only. Return `{id, updatedAt}`. ~-5 lines. [packages/storage/src/index.ts:53-58,93-99]
10. `shrink:` micro-batch — (a) `data-section-label` always equals `data-section`; drop attr + paper.ts fallback [ResumePreview.tsx, paper.ts]; (b) `= PAPER_PRESETS.a4` default params on `measureSheetPages`/`findPageCrossings` — every caller passes paper explicitly [paper.ts]; (c) `MIGRATED_FLAG` set in three branches → set once in `finally` [storage/index.ts]; (d) `selectDoc` selector with one caller → inline [store.ts]; (e) `themeCssVars` returns `Record<string,string>` forcing `as CSSProperties` at both call sites → return `CSSProperties` [themes.ts]. ~-12 lines.

net: -145 lines, -7 deps possible.
