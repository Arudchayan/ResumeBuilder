# ponytail-audit 5 — ResumeBuilder

1. `native:` hand-rolled dialog focus trap — 30-line useEffect cycling focusable elements, Escape handler, backdrop `<button>`. Native `<dialog>` + `showModal()` gives focus trap, Escape, and `::backdrop` free; keep only the body-scroll lock. [packages/ui/src/components/primitives.tsx:100-130]
2. `yagni:` per-package build ceremony for private never-published packages — `tsc -p` build scripts emitting `dist/` that nothing consumes (exports point at `src/index.ts`, vite aliases src, dist is gitignored), plus `lint` scripts byte-identical to `typecheck` in all six manifests, and per-package `test` scripts shadowed by the root vitest include. Drop build/lint/test from package manifests; keep one `typecheck`. [packages/*/package.json, apps/web/package.json]
3. `shrink:` `chips` IrBlock variant — every renderer flattens it to joined text anyway (preview joins with " · ", docx joins with " · ", compact path already emits plain paragraph). Emit `paragraph` from documentToIr; delete the union member and both render branches. [packages/templates/src/ir/documentToIr.ts:9,96-97,249-251, packages/templates/src/preview/ResumePreview.tsx:68-75, packages/export/src/docx/exportDocx.ts:50-57]
4. `yagni:` `LayoutIr.pages` array — `documentToIr` always returns exactly one page; pagination happens in DOM/CSS (measureSheetPages, print). Flatten to `columns: IrColumn[]`, delete `IrPage`, update the two `ir.pages[0]` consumers. [packages/templates/src/ir/documentToIr.ts:31-38,264-279, packages/templates/src/preview/ResumePreview.tsx:186, packages/export/src/docx/exportDocx.ts:197]
5. `shrink:` eight hardcoded theme `<option>`s duplicating the `themes` map keys/names (editor doesn't even import `themes`). Render `Object.entries(themes)` — one source of truth. [apps/web/src/pages/EditorPage.tsx:485-493]
6. `delete:` `themes[*].light` field + `--theme-light` var — emitted by `themeCssVars`, consumed nowhere (primary/dark/surface/gradient all are). Remove field and emission. [packages/ui/src/tokens/themes.ts, packages/ui/src/index.ts]
7. `delete:` `IndexedDbStorage.delete()` — zero callers anywhere (gallery offers no delete UI). Nothing. [packages/storage/src/index.ts:107-115]
8. `shrink:` `PageBreakGuides` renders three parallel `.map` loops over the same edges array (bands, indicators, badges). One loop emitting all three elements. [packages/templates/src/preview/PageBreakGuides.tsx:21-67]
9. `shrink:` `exportResumeJson`/`importResumeJson` — alias wrappers around `JSON.stringify`/`parseResumeData` parked in the storage package. Call them directly in store.ts; update round-trip test. [packages/storage/src/index.ts:124-131, apps/web/src/lib/store.ts:170,182]
10. `delete:` `exportStatus` state — PDF path nulls it one statement after setting it; DOCX shows "Preparing DOCX…" for a single await the disabled button already covers. Toasts suffice. [apps/web/src/pages/EditorPage.tsx:139,241,246,574]
11. `yagni:` speculative core re-exports with no external consumers — `TEMPLATE_IDS`, `type Job`, `type Project`, `getDefaultVisibility`, `getDefaultSectionOrder` are only used inside core. Trim `index.ts` to what's imported. [packages/core/src/index.ts]
12. `shrink:` `normalizeTemplateId` legacy branch (`modern|classic|default`) returns `"sidebar"` — identical to the fallthrough. Body collapses to a `TEMPLATE_IDS.includes` ternary. [packages/core/src/schema/resume.ts:139-145]
13. `shrink:` `crypto.randomUUID?.() ?? \`${prefix}-${Date.now()}\`` fallback armor — `randomUUID` is baseline in every supported browser. Plain `crypto.randomUUID()`. [packages/core/src/schema/sections.ts:31, packages/storage/src/index.ts:41]
14. `shrink:` duplicate skip-to-content link — App.tsx already renders one targeting `#main`, which EditorPage's own copy also targets; two consecutive skip links when editing. Keep App's. [apps/web/src/pages/EditorPage.tsx:332-334]
15. `delete:` no-op spread `...(ir.templateId === "compact" ? {} : {})` — both branches empty. Nothing. [packages/templates/src/preview/ResumePreview.tsx:226]
16. `shrink:` `await import("@resume/export")` inside `runExport` — store.ts already imports the package statically, so the dynamic import lazifies nothing. Static import. [apps/web/src/pages/EditorPage.tsx:243]
17. `native:` `"@resume/ui": "workspace:*"` dependency in the export package — exportDocx.ts imports only docx/@resume/core/@resume/templates. Delete the line. [-1 dep] [packages/export/package.json]

net: -146 lines, -1 deps possible.
