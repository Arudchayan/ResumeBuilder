# ponytail-audit 4 — ResumeBuilder

1. `delete:` e2e fixture `arudchayan-sample-2026.json` (259 lines) — zero references; smoke.spec loads only `arudchayan-resume.json`. Nothing. [apps/web/e2e/fixtures/arudchayan-sample-2026.json]
2. `delete:` `delivery-phases.md` (86 lines) — completed-rewrite phase log documenting since-deleted things (@resume/ports stubs, memory adapter, immer). Nothing. [docs/architecture/delivery-phases.md]
3. `yagni:` per-package tsc→dist build ceremony for private, never-published packages — vite already aliases every `@resume/*` to src, yet root build/pretest/typecheck chain six `--filter … build` steps (including a phantom `@resume/ports` that matches nothing). Point package exports at `src/index.ts` (or relative imports in export/templates), drop dist builds and the three chained root scripts. [package.json:10-15, packages/*/package.json]
4. `shrink:` five per-package `vitest.config.ts` files configuring the default (`environment: "node"` IS vitest's default), and web's adds an e2e exclude although web ships zero unit tests (`passWithNoTests`). Delete all five; keep plain `vitest run` in the three packages with tests, drop web's `test` script. [apps/web/vitest.config.ts, packages/*/vitest.config.ts]
5. `shrink:` three parallel setter mechanisms in `ResumeCommand` — typed `setTemplate`/`setTheme`/`setSectionVisibility`/`reorderSections`/`setSkills` cases alongside generic `setField` dot-paths that express all five. Keep `setField` + `setDocument`; retarget callers, drop the five variants and switch cases. [packages/core/src/commands/applyCommand.ts:6-11,31-56]
6. `yagni:` `peerDependencies` (react, react-dom) on private workspace packages ui + templates — nothing is published; devDeps already pin React 19. Delete the blocks. [packages/ui/package.json, packages/templates/package.json]
7. `shrink:` `buildSingleColumn` builds sidebar blocks then filters them back OUT by string-matching content (`text === "Profile"`, `=== doc.summary`, muted-heading sniff). Share the section loop with an `aside|main` target param; kill the post-filter. [packages/templates/src/ir/documentToIr.ts:216-241]
8. `delete:` dead exports/fields — `defaultTheme` (zero consumers), `Label` re-export (internal-only), core's `TEMPLATE_IDS`/`type Job`/`type Project`/`getDefaultVisibility` (unused outside core), `PageCrossing.sectionId` (UI prints `label` only; update paper.test.ts). Trim to what's imported. [packages/ui/src/tokens/themes.ts:81, packages/ui/src/index.ts, packages/core/src/index.ts, packages/templates/src/preview/paper.ts:27]
9. `shrink:` `themes[*].light` + `--theme-light` var — emitted by `themeCssVars`, consumed by nothing (surface IS consumed, keep it). Remove field + emission. [packages/ui/src/tokens/themes.ts, packages/ui/src/index.ts]
10. `shrink:` `exportResumeJson`/`importResumeJson` — near-alias wrappers around JSON.stringify/parse parked in the *storage* package. Inline into store.ts (call `parseResumeData` directly). [packages/storage/src/index.ts:122-131]
11. `shrink:` eight hardcoded theme `<option>`s in EditorPage duplicating the `themes` map keys/names. Render `Object.entries(themes)` — one source of truth. [apps/web/src/pages/EditorPage.tsx:490-497]
12. `native:` `crypto.randomUUID?.() ?? \`local-${Date.now()}\`` fallbacks — `randomUUID` is baseline in every supported browser (2021+); the `?.` + timestamp branches are dead armor. Plain `crypto.randomUUID()`. [packages/core/src/schema/sections.ts:44, packages/storage/src/index.ts:32,41]
13. `delete:` `exportStatus` state — PDF path nulls it one line after setting it; DOCX shows "Preparing DOCX…" for a single await the disabled button already covers. Toasts suffice. [apps/web/src/pages/EditorPage.tsx:139,246,251,581]
14. `shrink:` `normalizeTemplateId` — legacy branch (`modern|classic|default`) returns `"sidebar"`, identical to the fallthrough; body collapses to an `includes` ternary. [packages/core/src/schema/resume.ts:126-133]
15. `shrink:` `await import("@resume/export")` inside `runExport` — the package is already statically imported by store.ts, so the dynamic import lazifies nothing. Static import. [apps/web/src/pages/EditorPage.tsx:248]

net: -497 lines, -0 deps possible.
