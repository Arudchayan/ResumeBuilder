# ponytail-audit 3 — ResumeBuilder

1. `native:` whole jsPDF + html2canvas-pro capture/slice pipeline (275-line file + its 41-line test) — header already ships a Print button calling `window.print()`, the platform's PDF path; `resolveThemeColors`/`pickThemeColors` exist only to feed this capture. Browser print dialog; delete exportPdf.tsx, themeColors.test.ts, retarget e2e/export.spec.ts. [-2 deps] [packages/export/src/pdf/exportPdf.tsx, packages/export/src/__tests__/themeColors.test.ts]
2. `yagni:` duplicate content renderer `ClassicSidebarSheet` (428 lines) hand-building every section again beside `buildSidebarBlocks`, which already emits the same blocks as IR — preview alone bypasses the IR while DOCX consumes it. Render sidebar preview through existing `BlockView` + a two-column wrapper (~30 lines); drop the `templateId === "sidebar"` gate in ResumePreview. [packages/templates/src/preview/ClassicSidebarSheet.tsx, packages/templates/src/preview/ResumePreview.tsx:184-203]
3. `delete:` bundle of dead IR/metric fields — `LayoutIr.paper` (hardcoded, never read), `sectionAnchors` (never read), `IrColumn.width` (renderers use CSS grid / DXA constants), `kv.href` (computed tel:/mailto:, never rendered — preview kv drops it, DOCX kv drops it), `bullets` + `spacer` variants (never emitted, handled speculatively in both renderers), `entry.compact` (set for publications, ignored everywhere), `TemplateManifest.atsFriendly`/`supportsPhoto` (never read), `PaperPreset.jsPdfFormat` (export builds format from mm), `SheetPageMetrics.fillsFirstPage`/`widthMm` (never read), `PageCrossing.sectionId` (UI prints label only), `PdfExportOptions.name` (written, never read). Remove fields + their population sites. [packages/templates/src/ir/documentToIr.ts, packages/templates/src/preview/paper.ts, packages/export/src/pdf/exportPdf.tsx:12]
4. `delete:` stale `saas-seams.md` — documents `@resume/ports`, `NullAuthPort`, `LocalResumeLibrary`, all deleted before audit 2; root package.json scripts still chain `pnpm --filter @resume/ports build`, which matches nothing. Delete the doc; drop the `@resume/ports` filter from build/pretest/typecheck scripts. [docs/architecture/saas-seams.md, package.json:10-15]
5. `yagni:` three `@dnd-kit/*` packages + sensors/config/`SortableTocItem` (58 lines) to reorder an 11-item fixed list once. Native `<button>`s "Move up"/"Move down" calling `arrayMove` (~15 lines); kills PointerSensor/KeyboardSensor/collision setup. [-3 deps] [apps/web/src/pages/EditorPage.tsx:61-118,181-184,473-503]
6. `delete:` `isSectionEmpty` — 36-line exhaustive switch, zero callers since audit 2 flagged it. Nothing. [packages/core/src/schema/sections.ts:31-66]
7. `shrink:` `Field` vs `TextAreaField` — 24 duplicated lines differing only in element + min-height. One component taking `multiline?: boolean`. [packages/ui/src/components/primitives.tsx:43-93]
8. `shrink:` five near-identical `vitest.config.ts` files (four setting only the default `environment: "node"`, web adding an e2e exclude). One root config with `projects`; keep web's exclude. [apps/web/vitest.config.ts, packages/*/vitest.config.ts]
9. `shrink:` createObjectURL+click+revoke download dance copy-pasted 3× (pdf, docx, JSON export). One shared `downloadBlob(blob, filename)` in @resume/export. [packages/export/src/pdf/exportPdf.tsx:251-258, packages/export/src/docx/exportDocx.ts:239-245, apps/web/src/lib/store.ts:183-192]
10. `delete:` `reorderArray` command variant + case — zero dispatchers; the only drag-reorder targets sections (`reorderSections`). Nothing. [packages/core/src/commands/applyCommand.ts:14,75-80]
11. `delete:` `replacePresent(history, doc)` — returns a brand-new `{past:[],present,future:[]}`, ignoring its argument: it is `createHistory(doc)` under another name. Call `createHistory`. [packages/core/src/history/history.ts:41-43, apps/web/src/lib/store.ts:160]
12. `delete:` `exportPdfBlob` — pure forwarding alias of `exportPdfFromSheet`, zero callers. Call `exportPdfFromSheet` (or nothing, see #1). [packages/export/src/pdf/exportPdf.tsx:136-141]
13. `delete:` `isSectionEmpty`-adjacent core cruft — `validateResumeData` wrapper (zero callers; `parseResumeData` is the API), `customSections` schema field (`z.array(z.unknown())` defaulted, persisted, never rendered). Remove both; regenerate fixtures. [packages/core/src/schema/resume.ts:189,198-200, packages/core/src/schema/sections.ts:89]
14. `delete:` `getTemplate()` — exported finder with zero callers; both call sites do `TEMPLATES.find(...)`/`.map(...)` directly. Inline find. [packages/templates/src/ir/documentToIr.ts:343-345]
15. `delete:` `A4_PAPER` alias self-marked `@deprecated`, sole consumer is PageBreakGuides defaults. Use `PAPER_PRESETS.a4`. [packages/templates/src/preview/paper.ts:24-25, packages/templates/src/preview/PageBreakGuides.tsx:1]
16. `delete:` store `exporting` member — typed `"pdf"|"docx"|"json"|null`, initialized null, never set or read; EditorPage keeps local state. Local state covers it. [apps/web/src/lib/store.ts:34,73]
17. `delete:` unused UI exports — `defaultTheme` (zero consumers), `Label` re-export (internal-only, used by Field/TextAreaField), `TemplateThumb`'s `children` prop (GalleryPage passes accent only; the fallback art is the only branch ever taken). Trim exports and the prop. [packages/ui/src/tokens/themes.ts:69, packages/ui/src/index.ts:1-2, packages/templates/src/preview/ResumePreview.tsx:255-261]

net: -995 lines, -5 deps possible.
