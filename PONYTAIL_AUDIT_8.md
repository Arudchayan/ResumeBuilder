# ponytail-audit 8 — ResumeBuilder

1. `yagni:` the entire `skillsDensity` state machine — the value is read only by its own button highlight and a dep array; no renderer, IR builder, or CSS ever consumes it ("Compact skills" delivers a color change). Delete state, init, reset-effect, both setters, the toggle button, and `defaultSkillsDensity`/`SkillsDensity` behind them. Nothing; the button goes with the state. [apps/web/src/pages/EditorPage.tsx:145-146,165-167,265,303-307,642-648,669; packages/templates/src/preview/layoutAssist.ts:3-8; packages/templates/src/index.ts:20,23]
2. `native:` ui's hand-rolled `.font-display`/`.font-body` utility classes — web's Tailwind v4 `@theme` already generates `font-display` utilities from the same tokens; the ui copies (and the duplicate `--font-display`/`--font-body` :root defs) shadow them. Drop the ui blocks; Tailwind utilities remain. [packages/ui/src/styles.css:5-6,20-26]
3. `delete:` dead/duplicated CSS — `.rb-section` selectors (zero emitters anywhere), ui's second `prefers-reduced-motion` guard (strict subset of web's), ui's `.skip-to-content` block (byte-duplicate of web's, and app chrome doesn't belong in a lib sheet). Nothing; keep web's copies. ~-22 lines. [packages/ui/src/styles.css:27-34,64-73; apps/web/src/styles.css:590,982]
4. `yagni:` dependabot groups/patterns matching zero manifests — the whole `radix-ui` group, `@emotion/*`, `jsdom`, `@testing-library/*`, `postcss*`, `autoprefixer`, `@eslint/*`, `eslint*`, `@commitlint/*`, `cz-*`, `lint-staged`, `husky`, plus `versioning-strategy: increase` (the default). Keep the groups whose patterns match real deps (react-vendor, testing, build-tools, dev-dependencies trimmed). [\.github/dependabot.yml:22-52]
5. `delete:` committed build artifacts — `apps/web/dist/` (index.html + hashed JS/CSS) is tracked despite `dist` sitting in .gitignore; the Pages workflow rebuilds it fresh on every deploy. `git rm -r --cached apps/web/dist`. [apps/web/dist/]
6. `shrink:` vitest declared as a devDep in three packages while the root `test` script runs `vitest` without declaring it at all — it resolves only via `shamefully-hoist`. Declare vitest once in root devDependencies; delete the three package entries. [-3 manifest entries] [packages/core/package.json, packages/export/package.json, packages/storage/package.json, package.json]
7. `shrink:` nine preview-toolbar buttons each repeating `"!min-h-9 !rounded-lg !px-2.5 !py-1.5 text-xs"` to overpower Button's baked-in padding. Add a compact size (one more class string on Button); callers stop importing !important. [packages/ui/src/components/primitives.tsx:3-17; apps/web/src/pages/EditorPage.tsx:601,611,621,630,637,644,651,658,665]
8. `delete:` four orphaned `data-testid` attributes (`paper-meta`, `page-count-label`, `paper-size-detail`, `overflow-warnings`) — no spec references any testid; their specs died with the export e2e suite. Nothing. [apps/web/src/pages/EditorPage.tsx:546,553,554,564]
9. `delete:` storage tsconfig `compilerOptions.lib` block byte-identical to tsconfig.base — `extends` already inherits it; core's plain extends+include is the model. [packages/storage/tsconfig.json]
10. `delete:` drag-era residue — `.toc-grip { cursor: grab }` on click-only Move up/down buttons and the "Drag to change its order" hint; dnd-kit was removed in audit 3. `cursor: default`; reword the hint to moving. [apps/web/src/styles.css:323-325; apps/web/src/pages/EditorPage.tsx:435]
11. `shrink:` micro-batch — (a) `as TemplateId` casts on `t.id`, already a `TemplateId` [apps/web/src/pages/GalleryPage.tsx:75-76]; (b) vite `build` block restating defaults (`outDir: "dist"`, `sourcemap: false`) [apps/web/vite.config.ts:11-14]; (c) `.preview-stage.is-inspecting` re-declaring base `min-height`/`padding` verbatim [apps/web/src/styles.css:483-486]; (d) pdf branch computing a sanitized `filename` it never uses [apps/web/src/pages/EditorPage.tsx:242-245]; (e) `strict-peer-dependencies=false` restating pnpm's default [.npmrc]; (f) `keywords` search metadata on a `private: true` root package [package.json:15-22]; (g) empty `docs/architecture/` directory — rmdir. ~-20 lines.

net: -110 lines, -0 deps possible.
